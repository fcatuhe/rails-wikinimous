<h2><%= @article.title %></h2>
<%= Kramdown::Document.new(@article.content).to_html.html_safe %>
<button><%= link_to "Edit", edit_article_path %></button>
<button><%= link_to "Delete", article_path, method: :delete, data: { confirm: "Are you sure?"} %></button>
